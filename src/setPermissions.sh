#!/bin/bash

FOLDER_PATH = /usr/httpServer

chmod +x ${FOLDER_PATH}/httpServer # Grant permissions to the executable
chmod -R +w ${FOLDER_PATH}         # Grant write permissions to the executable's folder and its contents